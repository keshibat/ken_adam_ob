<%= form_with(model: product_purchased_listing, local: true) do |form| %>
  <% if product_purchased_listing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_purchased_listing.errors.count, "error") %> prohibited this product_purchased_listing from being saved:</h2>

      <ul>
      <% product_purchased_listing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_name %>
    <%= form.text_field :product_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :was_price %>
    <%= form.number_field :was_price %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="field">
    <%= form.label :reference_number %>
    <%= form.text_field :reference_number %>
  </div>

  <div class="field">
    <%= form.label :expiry_date %>
    <%= form.date_select :expiry_date %>
  </div>

  <div>
      <%= form.label :picture  %>
      <%= form.file_field :picture, accept: "image/png,image/gif,image/jpeg,image/jpg"   %>
  </div>

  <%= form.hidden_field :user_id, :value => params[:id] %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
